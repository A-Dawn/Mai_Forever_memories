# Changelog

本文件记录了 Mai Forever Memories 插件的所有重要变更。

## [0.2.2] - 2026-01-10

### 修复
- **定时导入功能失效**: 修复定时摘要无法自动导入到LPMM知识库的问题
  - 问题1：子进程中缺少 `model_config` 模块导入，导致 `ModuleNotFoundError`
  - 修复1：在 `_import_raw_summary_blocking` 方法中添加 `from src.config.config import global_config, model_config` 导入
  - 问题2：子进程脚本 `subprocess_worker.py` 中使用错误的导入路径 `chat.knowledge` 而不是 `src.chat.knowledge`
  - 修复2：修正子进程脚本中的导入路径为 `src.chat.knowledge`
- **配置问题**: 修复配置文件中 `auto_import = false` 导致的导入禁用问题
  - 将 `summary.auto_import` 设置为 `true` 以启用自动导入功能

### 技术细节
- **版本升级**: 从 0.2.1 升级到 0.2.2
- **代码变更**: 修改 2 个文件，添加必要的模块导入
- **配置修复**: 修复自动导入功能的配置设置

---

## [0.2.1] - 2026-01-07

### 修复
- **消息ID更新失败**: 修复确认导入机制中"更新消息ID失败"的错误
  - 问题：在发送确认消息时未指定 `storage_message=False` 参数，导致框架尝试存储消息并更新ID时失败
  - 修复：在 `_send_confirm_message` 方法中为 `send_api.send_text_message` 调用添加 `storage_message=False` 参数

### 技术细节
- **版本升级**: 从 0.2.0 升级到 0.2.1
- **代码变更**: 修改 1 行代码，修复消息处理问题

---

## [0.2.0] - 2025-01-04

### 新增
- **只总结不导入模式**: 新增 `summarize_*` 命令系列，允许仅生成摘要而不导入知识库
  - `/memories summarize_daily`: 仅总结每日摘要
  - `/memories summarize_weekly`: 仅总结每周摘要
  - `/memories summarize_now`: 仅总结永久记忆
- **确认导入机制**: 新增导入确认功能，在导入摘要到知识库前向管理员发送确认请求
  - `/memories confirm <task_id> <yes/no>`: 管理员确认或拒绝待导入任务
  - 支持超时自动拒绝机制
  - 可配置确认消息的目标流和超时时间

### 配置项新增
- `summary.auto_import`: 是否自动导入摘要到知识库（默认 `true`）
- `summary.confirm_import`: 是否启用导入确认机制（默认 `false`）
- `summary.confirm_stream`: 发送确认消息的目标流ID（默认为空，使用 `performance.admin_id`）
- `summary.confirm_timeout`: 确认超时时间（秒，默认 `300`）

### 优化
- **配置格式化**: 改进了 `allowlist` 和 `keywords` 的数组格式
- **数值格式**: 移除了 CPU 和内存阈值后的 `.0` 后缀

### 技术细节
- **版本升级**: 从 0.1.3 升级到 0.2.0
- **代码重构**: 大幅重构 plugin.py，新增 631 行代码，修改 26 行
- **向下兼容**: 保持与上游仓库的兼容性，新功能均为可选

---

## [0.1.3] - 2025-12-29

### 新增
- **依赖管理**: 添加 `requirements.txt` 文件，引入 `psutil>=5.9.0` 依赖，用于性能监控功能
- **子进程工作脚本**: 新增 `subprocess_worker.py`，独立的子进程脚本用于执行知识库重建任务，避免Pickling问题

### 优化
- **内容预处理**: 新增 `_preprocess_chat_content` 方法，智能过滤聊天内容中的插件回复、系统消息和格式化状态报告
  - 过滤大量表情符号（通常为插件输出）
  - 过滤格式化的状态报告消息（如 ❌✅📋 等）
  - 过滤固定的确认/错误消息模式
  - 过滤以特定关键词开头的插件响应

- **摘要生成质量**: 显著改进LLM摘要生成的prompt模板
  - 添加更详细的质量要求和指导
  - 明确标注关键时间节点和事件
  - 强化实体间关系的表达
  - 突出重要决策、共识和后续行动计划
  - 要求忽略命令消息、插件回复和系统通知

- **子进程处理机制**: 重构子进程处理方式，从ProcessPoolExecutor改为直接subprocess调用
  - 避免复杂的Pickling序列化问题
  - 提高跨平台兼容性
  - 简化错误处理和调试

- **跨平台兼容性**: 修复Windows环境下的编码问题
  - 设置UTF-8编码支持
  - 修复控制台输出编码问题
  - 改进路径处理和模块导入

### 修复
- **子进程稳定性**: 解决子进程执行时的序列化和导入问题
- **编码兼容性**: 修复Windows系统下的UTF-8编码处理
- **模块导入**: 修复子进程中的相对导入路径问题

### 技术细节
- **版本升级**: 从 0.1.2 升级到 0.1.3
- **架构改进**: 采用独立的子进程脚本架构，提高系统稳定性
- **性能监控**: 增强性能监控能力，支持更好的资源管理

---

## [0.1.2] - 2025-12-XX

### 修复
- 修复重复引用问题

## [0.1.1] - 2025-12-XX

### 新增
- 添加 `/memories now` 命令，支持立即生成永久记忆
- 改进子进程处理机制

### 重构
- 将重量级模块的导入方式重构为按需加载

## [0.1.0] - 2025-12-XX
- 永远的记忆插件正式发布
- 支持定时自动摘要功能
- 支持LPMM知识库集成
- 支持性能监控与安全防护

---

**注意**: 此变更日志基于上游仓库的提交历史和当前工作目录的修改内容整理。如有遗漏或需要补充，请及时反馈。
